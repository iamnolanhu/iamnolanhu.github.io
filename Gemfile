# frozen_string_literal: true

source "https://rubygems.org"

# GitHub Pages bundles Jekyll, jekyll-theme-hacker, and jekyll-seo-tag.
gem "github-pages", group: :jekyll_plugins

# Local link checking (script/cibuild uses the v3 --url-ignore flag)
gem "html-proofer", "~> 3.0", group: :test